import React from "react";
import pageLoader from "../assests/pageloader.gif";
const PageLoader = () => {
  return (
    <div>
      <img src={pageLoader} />
    </div>
  );
};

export default PageLoader;
